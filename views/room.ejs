<!DOCTYPE html>
<html lang="he" dir="rtl">

    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>חדר <%= roomNumber %> - Schedule</title>
        <link rel="stylesheet" href="/style.css" />
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
        <style>
            .room-detail-page {
                background: url('/room_image.png') no-repeat center center;
                background-size: 80% auto;
                min-height: 100vh;
                padding-top: 56px;
            }
        </style>
    </head>

    <body class="room-detail-page">

        <%- include('partials/navbar') %>
        <div style="height:56px;"></div>

        <div class="container-lg">
            <div class="d-flex justify-content-between align-items-center mt-4 mb-3">
                <h1 class="mb-0">חדר <%= roomNumber %></h1>
                <div class="current-time text-muted">
                    <%= moment().tz('Asia/Jerusalem').format('HH:mm') %>
                </div>
            </div>

            <div class="status-container text-center p-3 rounded">
                <% if (currentTherapist) { %>
                <span><%= currentTherapist.name %> בטיפול עד <%= currentTherapist.endTime %></span>
                <% } else { %>
                <span>החדר פנוי</span>
                <% } %>
            </div>
        </div>

        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.0/jquery.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        <script src="/script.js"></script>
    </body>

</html>